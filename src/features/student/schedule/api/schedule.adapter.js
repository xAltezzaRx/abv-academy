/**
 * @param {any} dto
 * @returns {import("../model/types").ScheduleDay[]}
 */
export const mapScheduleWeek = (dto) => {
    // Пока у нас заглушка — просто вернём dto как есть,
    // но место уже готово для реального маппинга.
    return dto;
};
